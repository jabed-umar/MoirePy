site_name: MoirePy
theme:
  # name: material
  name: readthedocs
  # name: kpn
  # name: terminal
  # name: torillic

plugins:
  - search
  - mkdocstrings:
      handlers:
        python:
          inventories: []
          options:
            show_source: true

# extra_css:
#   - assets/style_find_theta.css

# extra_javascript:
#   - assets/script_find_theta.js

nav:
  - Home: index.md
  - Installation: installation.md
  # - Find a and b: find_theta/index.html
  - Angle Value Calculator: find_theta.md
  - API Reference:
      - Layers: api/layers.md
      - Moire: api/moire.md
      - Utils: api/utils.md
